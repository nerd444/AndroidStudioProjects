<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" >
    <item >
        <shape android:shape="rectangle"  >
            <corners android:radius="5dip" />
            <stroke android:width="1dip" android:color="#2A377C" />
            <gradient android:angle="-90" android:startColor="#2A377C" android:endColor="#2A377C" />
            <!-- 테두리 내부 색상 설정 -->
            <solid android:color="@android:color/white" />

            <!-- 테두리 내부 여백 설정 -->
            <padding
                android:bottom="10dp"
                android:left="10dp"
                android:right="10dp"
                android:top="10dp" />

            <!-- 테두리 설정: 두께, 색상 -->
            <stroke
                android:width="3dp"
                android:color="#2A377C" />
        </shape>
    </item>

</selector>